class Solution {
public:
    int buyChoco(vector<int>& prices, int money) {
        sort(prices.begin(),prices.end());
        int money1 = money-(prices[0]+prices[1]);
        if(money1>=0)
        return money1;
        else
        return money;

    }
};
